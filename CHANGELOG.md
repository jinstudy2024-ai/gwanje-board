# 바뀐 내용

내 사본이 몇 버전인지는 **앱 화면 오른쪽 위**(`v1.3` 처럼)에서 볼 수 있습니다.
여기 맨 위 번호와 다르면 옛 버전입니다. 올리는 방법은 [docs/1_설치_10분.md](docs/1_설치_10분.md) 의 "내 버전 확인하고 올리기"에 있습니다.

**시트 데이터는 그대로 유지됩니다.** 새로 사본을 만들 필요 없습니다.

## 1.3 — 2026-09-19

- **작업 드라이버 추가** (`작업/작업드라이버.py` + `.ps1`/`.sh` 껍데기). 보드의 `할일` 카드를 담당 AI(CLI)가 실제로 고치게 한다: `claim → 하는중 → 작업 → 끝`(실패 시 `할일`로 되돌림). 게시판 I/O는 드라이버가 전담해 앱 주소·토큰이 AI에게 넘어가지 않는다(자동토론 v1.3 원칙). 설정은 `client/gwanje.config.json` 의 `작업` 블록, 문서는 `docs/5_작업드라이버.md`. 비용 상한(최대카드)·실행 전 확인·담당 사전검증 포함. 앱 코드(`Code.gs`·`Index.html`)는 손대지 않은 클라이언트 도구다.


- **비용 안내 정정** — "돈 안 든다"가 자동토론까지 공짜인 것처럼 읽히던 문장을 고쳤다. 작업보드 본체는 무료지만, 자동토론은 유료 AI 구독이 있어야 함을 README·설치문서·설명서에 분명히 표시.

- **탭 순서를 일하는 순서에 맞춤** — 💬 토론 · 🗂 작업보드 · 🚦 신호등 · 📜 작업일지 · 📚 지시문.
  정한다 → 나눈다 → 잡는다 → 남는다 의 순서다. 토론이 맨 뒤에 있으면 이 도구를 "잠금 거는 프로그램"으로만 읽게 된다.
  처음 열 때 보이는 화면은 그대로 신호등이다(매일 쓰는 안전 확인 화면이라서).
- 사용 설명서 추가 — `docs/0_사용법.md`, `docs/gwanje-board-manual-ko.pdf` (PDF, 21쪽).

## 1.2 — 2026-09-19

- 앱 화면에 버전 표시. `action=version` 으로도 확인 가능.
- 설치 문서에 **예시 데이터 넣어 보기**를 별도 단계로 분리. 빈 화면을 고장으로 오해하는 걸 막기 위해서.
- 자동토론 **참가자 사전검증** — 시작 전에 각 CLI를 한 번 불러 실제로 대답하는지 확인하고, 못 하면 이유를 알리고 뺀다. 토론 중 연속 2회 무응답도 같게 처리.
- 앱 코드(`Code.gs`·`Index.html`) 머리에 저작권 표시 추가. 사본에는 LICENSE 파일이 따라가지 않기 때문.

## 1.1 — 2026-09-19

- 설치 문서 앞에 **계정 사전 확인**(회사·학교 계정은 관리자가 Apps Script를 막아뒀을 수 있음).
- **권한 경고 화면** 통과 방법을 단계로 명시.
- README에 **경로 선택표** — 작업보드만 쓸 사람과 자동토론까지 갈 사람을 갈랐다.
- 자동토론 **총 호출 수 상한**(`토론.최대호출`)과 시작 화면의 예상 호출 표시.
- 예시 프로젝트·행위자 이름을 중립적인 것으로 교체.
- 드라이버 구조 변경 — 게시판 읽기·쓰기를 전부 드라이버가 맡고 AI는 발언만 파일로 넘긴다. 한글 깨짐이 사라지고 토큰이 AI에게 넘어가지 않는다.

## 1.0 — 2026-09-19

- 첫 공개. 작업보드(신호등·보드·일지·지시문·토론) + 파이썬 클라이언트 + 자동토론 드라이버.
